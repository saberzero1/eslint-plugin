import { ESLintUtils } from "@typescript-eslint/utils";
type Options = [{
    isDesktopOnly?: boolean;
}?];
declare const _default: ESLintUtils.RuleModule<"lookbehind", Options, unknown, ESLintUtils.RuleListener>;
export default _default;
